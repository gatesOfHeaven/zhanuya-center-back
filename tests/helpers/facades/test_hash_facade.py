# from pytest import mark

# from utils.facades import hash


# @mark.parametrize(('to_hash'), [(1), (2), (0), (-1), (10**10), (-1 * 10**10)])
# def test_hashing(to_hash: str):
#     token = hash.it(id)